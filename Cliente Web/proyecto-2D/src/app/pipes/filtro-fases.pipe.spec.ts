import { FiltroFasesPipe } from './filtro-fases.pipe';

describe('FiltroFasesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFasesPipe();
    expect(pipe).toBeTruthy();
  });
});
